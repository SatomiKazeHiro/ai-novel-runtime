{
  "name": "@novel-runtime/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "cd ../../ && pnpm -r --filter \"./packages/*\" build",
    "dev": "tsx watch src/server.ts",
    "pretest": "cd ../../ && pnpm -r --filter \"./packages/*\" build",
    "test": "vitest run",
    "test:watch": "vitest",
    "prestart": "cd ../../ && pnpm -r --filter \"./packages/*\" build",
    "start": "node dist/server.js",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@fastify/env": "^5.0.0",
    "@fastify/swagger": "^9.0.0",
    "@fastify/swagger-ui": "^5.0.0",
    "@novel-runtime/ai-provider": "workspace:*",
    "@novel-runtime/knowledge-graph": "workspace:*",
    "@novel-runtime/memory-engine": "workspace:*",
    "@novel-runtime/prompt-runtime": "workspace:*",
    "@novel-runtime/scoring-engine": "workspace:*",
    "@novel-runtime/shared": "workspace:*",
    "bullmq": "^5.76.8",
    "dotenv": "^16.4.0",
    "es-toolkit": "^1.47.0",
    "fastify": "^5.0.0",
    "fastify-plugin": "^5.0.0",
    "ioredis": "^5.10.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
