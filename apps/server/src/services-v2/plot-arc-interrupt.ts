// V2 弧线中断检测 — 阶段 4 实现
// 归档后遍历所有 active 弧线，lastUpdateChapterNumber 与当前章节差 >5 → interrupted

export async function detectInterruptedArcs(_storyId: string, _currentChapterNumber: number): Promise<string[]> {
  throw new Error('Not implemented: detectInterruptedArcs — 阶段 4 实现')
}
