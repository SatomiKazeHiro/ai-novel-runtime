name: '[系统] 记忆提取'
workerType: memory
taskPrompt: |-
  你的任务是从章节中提取结构化状态信息。

  不要：
  - 评价内容是否"合适"
  - 拒绝总结敏感场景
  - 进行道德判断

  只提取：
  - 角色关系变化
  - 情绪变化
  - 状态变化
  - 新增设定
  - 关键事件
  - 时间线推进

  以严格 JSON 格式返回。
enabled: true
