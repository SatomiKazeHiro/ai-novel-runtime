# AI Novel Runtime 运行流程说明

> 本文用直白易懂的语言，描述从程序启动到用户写完两章期间，整个系统内部发生了什么、数据如何流动、以及 AI 在哪些环节被调用。
>
> 不纠结数据库表结构，只讲"谁调了谁、做了什么"。

---

## 一、程序启动后，后台自动做了什么？

当你运行 `pnpm dev` 启动后端时，系统会按以下顺序"自己把摊子铺好"：

### 1. 加载环境变量
程序首先读取项目根目录的 `.env` 文件，拿到数据库地址、服务端口号、DeepSeek API Key 等基础配置。

### 2. 准备任务队列
系统尝试连接 Redis（地址从 `.env` 读取）。
- **连上了**：使用专业的 BullMQ 队列。
- **没连上**：自动降级为内存队列（用定时器模拟异步，单机开发也能跑）。

此时会创建三个队列，但目前只有"生成队列"真正干活，另外两个（评分队列、记忆队列）先占个坑。

### 3. 连接数据库
通过 Prisma 连接数据库（开发环境是 SQLite 文件，零配置）。

### 4. 自动配置 AI 服务
如果 `.env` 里写了 `DEEPSEEK_API_KEY`，系统会自动往数据库里写入（或更新）一条 DeepSeek 的默认配置：
- 模型：`deepseek-chat`
- 地址：`https://api.deepseek.com`
- 上下文长度：64000 tokens

**换 API Key 只需改 `.env` 然后重启即可**，不用去数据库里改。

### 5. 自动导入写作人格
系统会扫描 `docs/profiles/` 目录下的所有 `.json` 文件（比如 `default.json`、`xianxia.json` 等），把里面的人格配置导入数据库。
- 已经存在的不覆盖。
- 新加的会自动入库。

### 6. 注册"生成处理器"
把"章节生成"的 worker 挂到队列上，这样后续用户点击"生成候选"时，队列知道该派谁来干活。

### 7. 启动 HTTP 服务
最后打开 3000 端口，前后端开始联调。

---

## 二、用户创建一本小说

### 用户操作
用户在"小说管理"页面点"新建小说"，填个标题（简介可选），保存。

### 系统做了什么
1. **前端**发一个 `POST /api/stories` 请求到后端。
2. **后端**只在数据库里新增一条"小说"记录，仅此而已。
3. 不会自动创建章节，不会自动绑定写作人格。
4. **前端**刷新列表，用户仍停留在当前页。

### 进入创作空间
用户需要手动点击小说那一行的"设计"按钮，前端才会跳转到小说内部的设计空间，默认先看到"角色管理"页面。左侧菜单可以切到"章节工作台"开始写章节。

---

## 三、开发第 1 章：从零到归档

### Step 1：创建根章节

用户在"章节工作台"点击"新建根章节"。
- 前端发请求到后端，后端在数据库创建一条章节记录，状态为 `draft`（草稿）。
- 根章节的 `parentChapterId` 是空的，序号从 1 开始。

### Step 2：配置章节信息

用户进入章节编辑页，需要填写：
- **大纲**：这一章要写什么情节。
- **场景设定**：地点、氛围、目标（可选但建议填，后续评分会参考）。
- **写作人格**：下拉选择一个 Profile（比如"默认人格"、"仙侠风"等）。

这些信息会实时保存到数据库，用户可以随时修改。

### Step 3：生成候选（第 1 次调用 AI，且可能连续调多次）

用户点击"生成默认候选（×3）"按钮。

#### 前端动作
发请求 `POST /api/chapters/:id/generate`，告诉后端：
- 我要 3 个候选版本
- 温度分别是 0.6（保守）、0.75（标准）、0.9（活跃）
- 用哪个写作人格

#### 后端动作——组装 Prompt
后端收到请求后，会做一件很关键的事：**把写这一章需要的所有上下文攒成一个超长 Prompt**。它会去数据库拉取：
- 故事的基本信息（标题、简介）
- 所有角色及其最新状态
- 世界观设定（Lore）
- 时间线事件
- 与这章相关的历史记忆（语义搜索，取最相关的前 20 条）
- 当前活跃的剧情弧线
- 上一章的图谱快照（如果有的话）

然后把这些信息按固定顺序组装成 9 层结构：
```
风格层 → 故事层 → 世界观层 → 角色层 → 场景层 → 记忆层 → 时间线层 → 剧情弧线层 → 输出层
```

每一层都有 Token 预算上限，如果太长会自动截断，防止撑爆模型上下文。

接着，系统把"写作人格"翻译成 System Message，把上面组装的 9 层内容翻译成 User Message，最终得到一个"编译好的 Prompt"。

#### 后端动作——创建 Draft 并入队
后端创建 3 条 Draft 记录，状态都是 `generating`（生成中），然后把生成任务丢进队列：
```
队列任务：generate-chapter
参数：3 个 draft 的 ID、编译好的 Prompt、3 个温度值
```

#### 队列处理器干活（真正调 AI 的地方）
处理器从队列里取出任务，用一个 `for` 循环逐个处理 3 个 draft：
1. 拿同一个编译好的 Prompt
2. 传入不同的温度（0.6 / 0.75 / 0.9）
3. 调用 DeepSeek API（`/v1/chat/completions`）
4. 把 AI 返回的正文写入对应的 draft

**注意**：3 个 draft 是**串行**生成的，不是同时发 3 个请求，目的是降低瞬时 API 压力。

无论成功还是失败，系统都会异步写一条"Prompt 调用日志"，记录发了什么、回了什么、花了多少 token、耗时多久。

#### 前端怎么知道生成完了？
前端启动一个**每 2 秒轮询一次**的定时器，不断问后端"这章的 draft 都好了吗"。当所有 draft 状态都变成 `completed` 或 `failed` 时，停止轮询，页面上展示出 3 张候选卡片。

> **本章第 1 个 AI 调用节点**：生成候选。3 个 draft = 最多调 3 次 AI API。

---

### Step 4：评分（第 2 次调用 AI，可选）

每个 draft 卡片上有个"评分"按钮。用户点击后：

#### 后端动作
1. 取出这个 draft 的内容。
2. 取出最近 2~3 个已归档章节的内容作为"文风参考"。
3. 取出章节的场景设定和写作人格。
4. 把它们塞进一个"评分 Prompt"里，让 AI 从 7 个维度打分：
   - 文风接近度（和前面章节像不像）
   - 大纲符合度（有没有跑题）
   - 场景符合度（地点氛围对不对）
   - 人格一致性（语气风格是否匹配）
   - 文笔质量
   - 情感张力
   - 节奏把控
5. 调用 AI，要求返回严格的 JSON 格式分数。
6. 如果 AI 调用失败，会用内置的"规则评分引擎"兜底（按字数、标点等简单规则算个分）。
7. 把最终分数保存到数据库，并显示在 draft 卡片上。

> **本章第 2 个 AI 调用节点**：评分。每个 draft 单独评一次，通常用户只给看中的几个评分。

---

### Step 5：选择最佳草稿

用户看完 3 个候选（和分数），点击最满意那个卡片上的"采用"按钮。

#### 后端动作（原子操作）
在一个数据库事务里：
1. 把这章**其他所有 draft** 标记为 `rejected`（淘汰）。
2. 把**被选中的 draft** 标记为 `selected`（选中）。
3. 把章节状态改成 `selected`，并把 draft 的正文内容回填到章节的 `content` 字段里。

#### 前端动作
把选中的正文内容显示在编辑区，用户可以继续手动修改。

> 注意：此时章节还没真正"归档"，只是选好了内容。

---

### Step 6：归档（第 3~5 次调用 AI，最重的一环）

用户改满意后，点击"归档"。归档意味着"这章定稿了"，系统会触发一系列"善后整理"工作。

#### 后端动作——阶段 1：提取（1 次 AI 调用，不写数据库）

系统调用 `extractAll`，**只用 1 次 AI 调用同时完成三件事**：
1. **记忆提取**：让 AI 读这章内容，提取出关键信息（主线事件、支线、角色情绪变化、新伏笔、关系变化、时间线天数、50 字摘要等）。
2. **图谱提取**：让 AI 提取 importance ≥ 6 的核心实体（人物、势力、事件、物品）和它们之间的关系。注意：这里提取的是**原始素材**（`extractedGraph`），未经整理。
3. **剧情弧线提取**：让 AI 分析当前活跃的剧情弧线进展如何、是否进入新阶段、有没有新悬念。

> **关键变化**：提取结果**暂存为纯数据对象**，不写入数据库。如果后续阶段失败，不会有任何脏数据残留。

> **本章第 3 个 AI 调用节点**：合并提取（记忆 + 图谱 + 剧情弧线），1 次。

#### 后端动作——阶段 2：图谱整理（1 次 AI 调用）

系统调用 `graph-organizer`：

**输入**：上一章的全局图谱（`previousSnapshot`）+ 本章原始提取的节点/边（`extractedGraph`）。

**AI 做两件事**：
1. **生成新的全局图谱**（`mergedGraph`）：把原始素材和全局历史做合并整理——相同节点合并 data、相同关系去重、冲突解决。这时，一个节点的属性或一条关系的描述可能已经融合了前几章的信息。这是给下一章用的"累计全局"。
2. **独立生成本章范围图谱**（`chapterGraph`）：**只基于本章 raw 提取**，做格式化和去重，**不合并全局历史 data**。节点 data 和边 relation 严格反映本章明确提及的内容。

> **本章第 4 个 AI 调用节点**：图谱整理，1 次。

**关键区别**：
- `extractedGraph`（提取阶段）= **毛坯素材**，直接从文本读出，格式可能不统一。
- `mergedGraph`（整理阶段）= **累计全局**，融合了所有历史章节的信息，供下一章使用。
- `chapterGraph`（整理阶段）= **本章纯净视图**，只反映本章内容，不夹带全局历史前提。

> 举个例子：第10章原文写"李四投靠张三"，提取阶段得到 raw 边"李四-投靠-张三"。即使前9章的全局里，李四和张三的关系已经被更新为更复杂的"背叛后被迫再次投靠"，`chapterGraph` 仍然保持本章的原始描述"投靠"，不会替换成全局中的复杂关系。只有 `mergedGraph` 才会保留那个复杂关系供下一章参考。

#### 后端动作——阶段 3：事务写入（所有数据库操作一次性提交）

前两阶段都是纯 AI 调用，不写数据库。现在进入**真实事务**：

```
prisma.$transaction(async (tx) => {
  1. 写入 Memory（章节/场景/全局记忆）
  2. 写入 CharacterBranchState（角色状态快照）
  3. 写入 TimelineEvent（时间线）
  4. 更新 Chapter.summary（摘要）
  5. 写入 PlotArc（剧情弧线）
  6. 写入 GraphNode/GraphEdge（知识图谱）
  7. 更新 Chapter.graphSnapshot / graphDelta
  8. 更新 Chapter.status = 'archived'
})
```

**保证**：
- 事务内任何一步失败 → 全部回滚，数据零变更
- 事务成功 → 所有数据一致提交，章节正式归档

> 之前这里是"伪事务"——AI 提取时边提取边写数据库，后续失败会导致脏数据。现在改为"先提取、后事务写入"，彻底解决了这个问题。

#### 后端动作——阶段 4：记忆优化（1 次 AI 调用，可选，失败不阻塞）

**每章归档都会触发**：
- 输入：上一章的 `global` 层记忆（优化后的状态快照）+ 本章新提取的 `chapter` 层原始记忆。
- AI 分析新旧记忆的关系，决定"继承 / 更新 / 新增 / 删除"。
- 重要规则：
  - **继承旧 UID**：同一事件的新进展，保留原来的 `originUid`（如 `3#A1B2`），系统会在 Prompt 里自动取最新版本。
  - **新增 UID**：全新事件分配新的 `originUid`。
  - **主角事件 +1 重要度**：AI 自评 `importance`（4~7），主角参与的事件再 +1。
- 结果以 `global` 层追加式插入数据库（同一 UID 可能有多条历史版本，Prompt 组装时只取最新）。

> **关键变化**：记忆优化**在事务之后**执行。如果优化失败，只打日志，不会回滚已归档的章节。因为 global 记忆是"锦上添花"，不是归档的必要条件。

> **本章第 5 个 AI 调用节点**：记忆优化。每章归档必触发，替代了旧的"每 5 章压缩 + 每章整理"。

---

## 四、开发第 2 章：从第 1 章发展而来

### Step 1：发展下一章

用户在章节树（Git Graph 风格的树状图）上，对着已归档的第 1 章点击"发展下一章"。

#### 后端动作
1. 校验：只能从 `archived` 或 `selected` 的章节发展。
2. 主线章节只能从"最新章节"发展（不能从中间插）。
3. 自动分配序号：第 2 章 = 第 1 章序号 + 1。
4. 在数据库创建新章节记录，`parentChapterId` 指向第 1 章，状态为 `draft`。

如果是"番外"，序号会分配为小数（比如 1.01），不占用主线位置。

### Step 2~6：重复同样的流程

第 2 章的诞生和第 1 章完全一样：
1. 用户配置大纲、场景、写作人格。
2. 点击生成候选 → 后端组装 Prompt → 队列串行生成 3 个 draft → 前端轮询展示。
3. （可选）给 draft 评分。
4. 用户选一个 draft"采用"，内容回填到章节，或直接粘贴外部正文。
5. 用户手动修改满意后点击"归档"。
6. 归档触发四阶段：
   - 阶段 1：`extractAll`（纯 AI 提取）
   - 阶段 2：`organizeGraph`（图谱整理）
   - 阶段 3：`prisma.$transaction`（事务写入所有数据 + 状态变 `archived`）
   - 阶段 4：`optimizeMemories`（记忆优化，失败不阻塞）

**关键区别**：
- 组装 Prompt 时，第 2 章会读取第 1 章归档时留下的**全局图谱快照**和**整理后的记忆**，所以 AI 写第 2 章时是"带着前情提要"在写。
- 每章归档都会触发记忆优化（不再是每 5 章一次）。

---

## 五、两章开发期间的 AI 调用全景图

| 阶段 | AI 调用次数 | 让 AI 做了什么 | 温度 |
|------|------------|---------------|------|
| **生成候选** | 3 次/draft（3个draft=3次） | 读超长 Prompt（故事+角色+世界观+记忆+大纲），写一章正文 | 0.6 / 0.75 / 0.9 |
| **评分** | 1 次/draft（可选） | 读草稿+前文参考，从 7 个维度打分并写评语 | 0.5 |
| **归档-合并提取** | 1 次 | 读章节内容，同时提取记忆+核心实体关系+剧情弧线进展 | 0.3 |
| **归档-图谱整理** | 1 次 | 读"上一章全局图谱+本章新提取"，合并成新的全局图谱 | 0.2 |
| **归档-记忆优化** | 1 次 | 读"上一章 global + 本章 raw"，融合生成新的 global 记忆快照 | 0.2 |

**归档流程概览**：

```
阶段 1: extractAll        → 纯 AI 提取（记忆+图谱+弧线），不写数据库
阶段 2: organizeGraph     → 纯 AI 整理图谱（mergedGraph + chapterGraph）
阶段 3: $transaction      → 一次性写入 Memory/CharacterBranchState/TimelineEvent/
                              PlotArc/GraphNode/GraphEdge + status='archived'
阶段 4: optimizeMemories  → 可选优化，失败不阻塞
```

- 阶段 1/2 失败 → 没有任何数据写入
- 阶段 3 失败 → 事务回滚，数据零变更
- 阶段 4 失败 → 归档已成功，仅全局记忆优化未执行

### 两章下来，AI 大概被调了多少次？

假设用户：
- 每章都生成 3 个候选
- 每章给 1 个 draft 评分
- 两章都正常归档

那么总共约 **11 次 AI 调用**：
- 生成：3 + 3 = 6 次
- 评分：1 + 1 = 2 次
- 归档提取：1 + 1 = 2 次
- 归档整理：1 + 1 = 2 次（图谱+记忆优化）

从第 1 章开始，每章归档都会触发记忆优化（不再是每 5 章一次）。

---

## 六、一句话总结各模块职责

| 模块 | 人话解释 |
|------|---------|
| **队列系统** | 生成任务的后台任务栏，没 Redis 就用内存冒充 |
| **Prompt 组装管道** | 像拼乐高一样，把角色/记忆/大纲等按固定顺序拼成一个超长 Prompt，每层有预算不怕超长 |
| **AI Provider** | 统一的外卖窗口，支持 DeepSeek / OpenAI / OpenRouter / Moonshot / SiliconFlow 等 OpenAI 兼容格式，负责把 Prompt 送出去、把正文拿回来 |
| **生成处理器** | 后厨厨师，接到订单后一个一个炒（串行生成 draft） |
| **合并提取器** | 归档时的"阅读理解课代表"，一次作业交出三份笔记（记忆+图谱+剧情） |
| **图谱整理** | 归档时的"地图更新员"，把新探明的区域画进总地图 |
| **记忆优化器** | 归档时的"笔记优化员"，每章都把新笔记和旧笔记融合成最新版总览，保留历史版本 |
| **评分系统** | 请 AI 当编辑，从 7 个维度给草稿打分，AI 不在线时就自己按字数估一个 |
| **Prompt 日志** | 所有 AI 对话的行车记录仪，成功失败都记录，方便查账 |
