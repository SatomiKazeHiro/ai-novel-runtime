# AI 小说工坊 运行流程说明

> 本文用直白易懂的语言，描述从程序启动到用户写完两章期间，整个系统内部发生了什么、数据如何流动、以及 AI 在哪些环节被调用。
>
> 不纠结数据库表结构，只讲"谁调了谁、做了什么"。

---

## 一、程序启动后，后台自动做了什么？

当你运行 `pnpm dev` 启动后端时，系统会按以下顺序"自己把摊子铺好"：

### 1. 加载环境变量
程序首先读取项目根目录的 `.env` 文件，拿到数据库地址、服务端口号、DeepSeek API Key 等基础配置。

### 2. 准备任务队列
系统尝试连接 Redis（地址从 `.env` 读取）。
- **连上了**：使用专业的 BullMQ 队列。
- **没连上**：自动降级为内存队列（用定时器模拟异步，单机开发也能跑）。

此时会创建三个队列，但目前只有"生成队列"真正干活，另外两个（评分队列、记忆队列）先占个坑。

### 3. 连接数据库
通过 Prisma 连接数据库（开发环境是 SQLite 文件，零配置）。

### 4. 自动配置 AI 服务
如果 `.env` 里写了 `DEEPSEEK_API_KEY`，系统会自动往数据库里写入（或更新）一条 DeepSeek 的默认配置：
- 模型：`deepseek-chat`
- 地址：`https://api.deepseek.com`
- 上下文长度：64000 tokens

**换 API Key 只需改 `.env` 然后重启即可**，不用去数据库里改。

### 5. 自动导入写作人格
系统会扫描 `seeds/profiles/` 目录下的所有 `.yaml` 文件（比如 `default.yaml`、`xianxia.yaml` 等），把里面的人格配置导入数据库。
- 已经存在的不覆盖。
- 新加的会自动入库。
- **YAML 解析失败会立即报错**，不会用空对象 fallback（这是与旧 JSON 时代的关键差异）。

### 6. 注册"生成处理器"
把"章节生成"的 worker 挂到队列上，这样后续用户点击"生成候选"时，队列知道该派谁来干活。

### 7. 启动 HTTP 服务
最后打开 3000 端口，前后端开始联调。

---

## 二、用户创建一本小说

### 用户操作
用户在"小说管理"页面点"新建小说"，填个标题（简介可选），保存。

### 系统做了什么
1. **前端**发一个 `POST /api/stories` 请求到后端。
2. **后端**只在数据库里新增一条"小说"记录，仅此而已。
3. 不会自动创建章节，不会自动绑定写作人格。
4. **前端**刷新列表，用户仍停留在当前页。

### 进入创作空间
用户需要手动点击小说那一行的"设计"按钮，前端才会跳转到小说内部的设计空间，默认先看到"角色管理"页面。左侧菜单可以切到"章节工作台"开始写章节。

---

## 三、开发第 1 章：从零到归档

### Step 1：创建一根"主干"章节

用户打开"章节工作台"，点"新建根章节"。
- 系统在数据库里建一条章节记录，状态为"草稿"。
- 根章节没有上一章，序号是 1。

> 这一步不调 AI，纯粹是建一条空记录。

### Step 2：填章节信息

用户进入章节编辑页，填三样东西（都可以随时改）：
- **大纲**：这一章要写什么情节。
- **场景设定**：地点、氛围、目标（可选，但建议填，后面的评分会参考）。
- **写作人格**：下拉选一个"风格人设"，比如"默认人格"或"仙侠风"。

这些信息实时存到数据库里。

### Step 3：让 AI 写 3 个候选版本（第 1 个 AI 节点）

用户点"生成默认候选（×3）"按钮。

#### 3.1 攒一个超长"上下文包裹"

后端去数据库把"写这一章需要知道的所有事情"全拉出来：
- 故事的基本信息（标题、简介）
- 所有角色及其最新状态
- 世界观设定
- 时间线事件
- 与这章相关的历史记忆（按相似度挑出最相关的 20 条）
- 当前活跃的剧情弧线
- 上一章留下来的"全书总图谱"（如果这是第 1 章就没有）

然后按固定顺序拼成 9 层（风格→故事→世界观→角色→场景→记忆→时间线→弧线→输出）。每一层有字数上限，太长会自动砍掉尾巴。

最后把"写作人格"翻译成 System Message，把那 9 层翻译成 User Message，得到一份"编译好的长指令"。

> 这一步的产物只是文字，没有调 AI，也没有写库。

#### 3.2 建 3 张"草稿卡" + 排进任务栏

后端建 3 条草稿记录，状态都是"生成中"，然后把"用这份长指令生成 3 个版本"这件事丢进任务栏：
- 草稿 1：温度 0.6（偏保守）
- 草稿 2：温度 0.75（标准）
- 草稿 3：温度 0.9（更活跃）

#### 3.3 后厨干活：真的调 AI

任务栏里的工人把这 3 张草稿**一个接一个**处理（不是 3 个一起发请求）：
1. 拿同一份长指令
2. 用不同的温度调 AI
3. 把 AI 返回的正文写回对应的草稿

> 串行处理是为了避免同时给 API 厂商打 3 个请求，把它压垮。

无论这张草稿成功还是失败，系统都会悄悄记一行"调用日志"——发了什么、回什么、花了多少字、花了多久，方便后面查账。

#### 3.4 前端怎么知道"活儿干完了"？

前端开一个"每 2 秒问一次"的定时器，反复问后端"这 3 张草稿都好了吗"。等所有草稿都变成"完成"或"失败"，停止询问，页面上亮出 3 张候选卡片。

> **本章第 1 个 AI 调用节点**：生成 3 个候选。3 张草稿最多 = 3 次调 AI。

---

### Step 4：给看中的草稿打个分（第 2 个 AI 节点，可选）

每张草稿卡片上有个"评分"按钮。用户挑几张想评的，点一下。

后端做这些事：
1. 取出这张草稿的正文。
2. 取出最近 2~3 个已归档章节的正文当"文风参考"。
3. 把它们塞进一份"评分指令"，让 AI 从 7 个维度打分：文风像不像前面、大纲有没有跑题、场景对不对、语气匹不匹配、文笔、情感张力、节奏。
4. 让 AI 必须按 JSON 格式返回分数。
5. 如果 AI 这边出故障（断网、超时等），自动降级用"字数+标点"的简单规则算个分兜底。
6. 把最终分数存到数据库，显示在草稿卡片上。

> **本章第 2 个 AI 调用节点**：评分。每张草稿独立评一次。

---

### Step 5：挑一张最满意的"采用"

用户看完 3 张草稿（和分数），点最满意那张的"采用"按钮。

后端做这一件事（一次性提交，保证不出岔子）：
1. 把另外两张草稿标记为"淘汰"。
2. 把选中的这张标记为"已选中"。
3. 把章节状态变成"已选中"，并把草稿正文回填到章节正文字段。

> 章节到这里还**没**真正"归档"，只是选好了内容，状态是"已选中"。

---

### Step 6：归档（最重的一环：第 3、4、5 个 AI 节点）

用户对选中的正文改满意后，点"归档"。归档=这章定稿了。

但这里多了一道"人工把关"环节——不是直接让系统埋头干完，而是先把 AI 的产出摆出来给你看，你确认/改完后再真正落库。整段流程拆成 5 个阶段。

#### 阶段 1：让 AI 读一遍正文，提取 3 类原始素材（1 次 AI 调用，**不写库**）

调一次 AI，让它**一口气做完三件事**：
1. **记忆提取**：从这章正文里挑出关键信息——主线事件、支线、角色情绪变化、新埋伏笔、关系变化、时间线是第几天、50 字摘要。
2. **图谱提取**：从正文里挑出"重要度 ≥ 6"的人物、势力、事件、物品，以及它们之间的关系。注意：这是**原始素材**，还没整理。
3. **剧情弧线进展**：看当前活跃的剧情弧线走到哪了、有没有进入新阶段、有没有新悬念。

> 这份提取结果**只放在内存里**，不写库。如果后面任何一步出问题，这份数据就当没发生过。

> **本章第 3 个 AI 调用节点**：合并提取，1 次。

#### 阶段 2：把"本章新发现的"合并进"全书总图谱"（1 次 AI 调用）

输入：上一章留下来的"全书总图谱" + 本章刚提取的原始节点/边。

AI 做两件事：
1. **生成新的"全书总图谱"**：把新素材和历史全局合并整理——相同节点合并属性、相同关系去重、冲突解决。节点属性或关系描述可能已经融合了前几章的信息。这是给下一章用的"累计版"。
2. **独立生成本章专属图谱**：**只基于本章原文提取**做格式化和去重，**不混入历史信息**。节点属性和边关系严格反映本章明明白白写到的内容。

> 两者区别打个比方：第 10 章原文写"李四投靠张三"。即使前 9 章的全局里，李四和张三的关系已经被整理成更复杂的"背叛后被迫再次投靠"，"本章专属图谱"仍然老老实实记"投靠"，不会被全局的复杂版本替换。只有"全书总图谱"才保留那个复杂版本给下一章参考。

> **本章第 4 个 AI 调用节点**：图谱整理，1 次。

#### 阶段 2.5：人工审查（**新增环节**）

阶段 1 和阶段 2 跑完后，系统**不直接落库**，而是把上面产出的"待归档数据"先存进章节记录本身的一个字段里，同时把章节状态变成"待审查"。

然后打开一个叫"审查面板"的页面，让你**逐项检查 AI 的产出**：
- 提取的记忆对不对？多了/少了/写错了可以直接改
- 全书总图谱的节点/边合并得合不合理？
- 本章专属图谱对不对？
- 剧情弧线的进展写得对不对？
- 角色状态快照对不对？
- 时间线事件对不对？

你每改一次，前端会自动把改动存回那个"待归档数据"字段（带防抖，不会每秒都打）。这一阶段可以**反复进出**，改了又改都行，AI 不会重跑。

**取消归档** = 直接删除这章（章节记录和草稿都清掉，从头再来）。
**确认归档** = 进入阶段 3。

> 这一步**不调 AI**，纯人工编辑。

#### 阶段 3：一次性落库（不调 AI，但最关键）

进入"确认归档"后，系统把"待归档数据"在**一个数据库事务里**一次性提交，包括：
- 记忆（章节级、场景级、全局级）
- 角色状态快照
- 时间线事件
- 章节摘要
- 剧情弧线
- 图谱节点/边
- 章节的"全书总图谱快照"和"本章专属图谱"
- 章节状态变成"已归档"

**为什么叫"一次性"？**
- 写入前会先把数据准备好（把"李四"这种角色名解析成数据库里真实的角色 ID）。
- 准备好的数据塞进事务里，**任一步失败 → 全部回滚**，数据库零变更。
- 全部成功 → 章节正式归档。

特别说明：
- 角色状态不是直接覆盖，而是先读最新状态再"浅合并"——新属性补进去，旧属性不丢。
- 同一天的时间线事件会自动累加到旧事件列表里，不会冲掉。

> 这一步**不调 AI**，是纯数据库操作，但它是整段流程最关键的"落锤"环节。

#### 阶段 4：再让 AI 整理一次"全局笔记"（1 次 AI 调用，失败不阻塞）

事务提交、章节正式归档之后，系统**再调一次 AI**：
- 输入：上一章的"全局笔记"（优化过的状态快照）+ 本章新提取的"章节级"原始笔记。
- AI 分析新旧笔记的关系，决定"继承 / 更新 / 新增 / 删除"。
- 关键规则：
  - 同一事件的新进展，**沿用旧的编号**——比如旧的编号是 `3#A1B2`，新进展也用这个号。
  - 全新事件才分配新编号。
  - AI 自己给每条笔记打分（4~7），主角参与的事件自动 +1。
- 结果以"全局"层追加式写进数据库。同一个编号可能有多条历史版本，组指令时只取最新那条。

> 关键设计：这一步**在事务之后**才跑。如果这一步失败，只记日志，**不会回滚**已经归档的章节。因为"全局笔记"是锦上添花，归档的硬骨头已经在阶段 3 啃完了。

> **本章第 5 个 AI 调用节点**：记忆优化。每章归档都触发。

---

## 四、开发第 2 章：从第 1 章发展而来

### Step 1：发展下一章

用户打开"章节树"（Git Graph 风格的树状图），对着第 1 章点"发展下一章"。

后端做这些事：
1. 校验：只有"已归档"或"已选中"的章节才能往下长新章节。
2. 主线章节只能从"最新那一章"往下长（不能从中间插队）。
3. 自动分配序号：第 2 章 = 第 1 章的序号 + 1。
4. 在数据库里建一条新章节记录，状态为"草稿"，并指向第 1 章为"父章节"。

> 如果是"番外"（支线故事），序号会是小数（比如 1.01），不占主线位置。

### Step 2~6：流程跟第 1 章一模一样

第 2 章从生到死和第 1 章完全一样：
1. 用户填大纲、场景、写作人格。
2. 点生成候选 → 后厨串行生成 3 个草稿 → 前端轮询展示。
3. （可选）给看中的草稿打个分。
4. 选一张草稿"采用"（或者干脆从外部粘贴正文），内容回填到章节。
5. 用户手动改满意后点"归档"，进入"审查面板"逐项检查 AI 提取的记忆、图谱、弧线、角色状态、时间线，必要时直接改。
6. 确认归档后：
   - 待归档数据一次性提交（事务内）→ 状态变"已归档"
   - 跑一次"全局笔记优化"（失败不阻塞）

**和第 1 章的关键区别**：
- 攒"上下文包裹"时，第 2 章会读到第 1 章归档时留下的"全书总图谱"和整理后的记忆，所以 AI 写第 2 章时是"带着前情提要"在写。
- 每章归档都跑一次记忆优化（不再是"每 5 章一次"）。

---

## 五、两章开发期间的 AI 调用全景图

| 阶段 | AI 调用次数 | 让 AI 做了什么 | 温度 |
|------|------------|---------------|------|
| **生成候选** | 3 次/章 | 读超长指令（故事+角色+世界观+记忆+大纲），写一章正文 | 0.6 / 0.75 / 0.9 |
| **评分** | 1 次/草稿（可选） | 读草稿+前文参考，从 7 个维度打分并写评语 | 0.5 |
| **归档-合并提取** | 1 次/章 | 读章节正文，同时提取记忆+核心实体关系+剧情弧线进展 | 0.3 |
| **归档-图谱整理** | 1 次/章 | 读"上一章全书总图谱+本章新提取"，合并成新的全书总图谱 | 0.2 |
| **归档-记忆优化** | 1 次/章 | 读"上一章全局笔记+本章原始笔记"，融合成新的全局笔记 | 0.2 |

> 注意：第 3、4、5 行只在用户走到"归档"流程时才会触发。中间任何一个环节**不调 AI**：攒上下文包裹、写草稿记录、串行生成草稿、采用/淘汰草稿、人工审查、提交事务，这些都是纯工程操作，不花钱。

**归档流程总览**（5 个阶段）：

```
阶段 1: 让 AI 提取 3 类原始素材       → 纯 AI 提取，不写库
阶段 2: 让 AI 把新素材合并进总图谱   → 纯 AI 整理，不写库
阶段 2.5: 人工审查 + 改              → 不调 AI，纯编辑
阶段 3: 待归档数据一次性提交         → 不调 AI，纯数据库事务
阶段 4: 让 AI 再整理一次全局笔记     → 纯 AI 优化，失败不阻塞
```

- 阶段 1 / 2 失败 → 没有任何数据写入数据库
- 阶段 2.5 取消 → 章节直接删除（"取消归档"=从零再来）
- 阶段 3 失败 → 事务回滚，数据零变更
- 阶段 4 失败 → 归档已成功，仅全局笔记优化没跑

### 两章下来，AI 大概被调了多少次？

假设用户：
- 每章都生成 3 个候选
- 每章给 1 张草稿评分
- 两章都正常归档（中途没取消重来）

那么总共约 **11 次 AI 调用**：
- 生成：3 + 3 = 6 次
- 评分：1 + 1 = 2 次
- 归档提取：1 + 1 = 2 次
- 归档整理：1 + 1 = 2 次（图谱整理 + 记忆优化）

> 从第 1 章开始，每章归档都会触发记忆优化（不再是"每 5 章一次"了）。

---

## 六、一句话总结各模块职责

| 模块 | 人话解释 |
|------|---------|
| **任务栏** | 生成任务的后台任务栏，连不上 Redis 就用内存冒充 |
| **指令组装管道** | 像拼乐高一样，把角色/记忆/大纲等按固定顺序拼成一份超长指令，每层有字数上限不怕撑爆 |
| **AI Provider** | 统一的外卖窗口，支持 DeepSeek / OpenAI / OpenRouter / Moonshot / SiliconFlow 等 OpenAI 兼容格式，负责把指令送出去、把正文拿回来 |
| **生成处理器** | 后厨厨师，接到订单后一个一个炒（串行生成草稿） |
| **合并提取器** | 归档时的"阅读理解课代表"，一次作业交出三份笔记（记忆+图谱+剧情） |
| **图谱整理** | 归档时的"地图更新员"，把新探明的区域画进总地图 |
| **审查面板** | 归档前的"质检岗"，让用户逐项检查 AI 提取的记忆/图谱/弧线，能直接改 |
| **记忆优化器** | 归档时的"笔记优化员"，每章都把新笔记和旧笔记融合成最新版总览，保留历史版本 |
| **评分系统** | 请 AI 当编辑，从 7 个维度给草稿打分，AI 不在线时按字数兜底估一个 |
| **指令日志** | 所有 AI 对话的行车记录仪，成功失败都记，方便查账 |
